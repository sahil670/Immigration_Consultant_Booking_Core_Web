@page
@model Immigration_Consultant_Booking_Core_Web.Pages.ConsultationBookings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Consultant Bookings</h1>

<p>
    <a class="btn btn-primary"  asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConsultationBooking[0].Consultant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsultationBooking[0].Client)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConsultationBooking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Consultant.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.Name)
            </td>
            <td>
                <a class="btn btn-primary"  asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-primary"  asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-primary"  asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
